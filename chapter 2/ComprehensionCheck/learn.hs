-- module Learn where
-- First, we declare the name of our module so
-- it can be imported by name in a project.

-- x = 10 * 5 + y
-- myResult = x * 5
-- y = 10


--Note: Order of declaring doesnt matter in a source file.

module Learn where
-- First declare the name of our module so it
-- can be imported by name in a project.
-- We won't do this for a while yet.
x = 10
 * 5 + y   --when you break up lines of code that the second
            --line begins *at least*(1 or more) one space from the beginning of that line
myResult = x * 5
y = 10