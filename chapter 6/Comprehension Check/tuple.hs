-- quotRem type:
ghci> :t quotRem
quotRem :: Integral a => a -> a -> (a, a)


-- 2) divMod
ghci> :t divMod
divMod :: Integral a => a -> a -> (a, a)

Takes two inputs of integral type and returns a tuple of the same type as output